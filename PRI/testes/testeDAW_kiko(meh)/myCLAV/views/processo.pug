extends layout

block content
  .w3-container
    .w3-card-4
      header.w3-container.w3-indigo
        center
          h1 Clav
      .w3-container
        center
          h3= processo[0].titulo
        h4 Código: #{processo[0].codigo}
        h4 Status: #{processo[0].status}
        p= processo[0].desc
      .w3-container
        //- table.w3-table-all
          th.w3-indigo HEADER
          each elem in lista
            tr
              td
                h4
                  //- a(href="http://clav-test.di.uminho.pt/api/classes/c"+elem.codigo) #{elem.codigo}
                  a(href="localhost:6001/"+elem.codigo) #{elem.codigo}
                h4 #{elem.titulo}
                p= elem.id